# MQTT Simple Client

The title says it all - basic tool, that enables to connect to MQTT broker, subrcribe to topics,
publish messages and display the received ones in the special terminal frame. That is all.
